<div class="steam-section">
  <div class="steam-section-header">
    Capsule Image Analysis
  </div>
  <div class="steam-section-content">
    <div class="steam-comparison">
      <!-- Current Version -->
      <div class="steam-version">
        <div class="version-header current">
          Current Steam Page
        </div>
        <div class="version-content">
          <div class="content-block">
            <h6>Capsule Image</h6>
            <% if @game.capsule_image_url.present? %>
              <img src="<%= @game.capsule_image_url %>" alt="Game capsule image" class="img-fluid rounded mb-3" style="max-width: 460px;">
            <% else %>
              <p class="text-muted">
                <i class="fas fa-exclamation-circle"></i> No capsule image uploaded
              </p>
            <% end %>
          </div>
        </div>
      </div>

      <!-- AI-Optimized Version -->
      <div class="steam-version">
        <div class="version-header optimized">
          Optimized Version
        </div>
        <div class="version-content">
          <div class="content-block">
            <h6>AI Recommendations</h6>
            <% if @analysis.image_suggestions.present? %>
              <div class="content-with-copy">
                <div class="content">
                  <ul>
                    <% @analysis.image_suggestions.split("\n").each do |suggestion| %>
                      <% if suggestion.strip.present? %>
                        <li><%= suggestion.strip %></li>
                      <% end %>
                    <% end %>
                  </ul>
                </div>
              </div>
            <% elsif @game.capsule_image_url.present? %>
              <p>
                <i class="fas fa-exclamation-circle"></i> No AI suggestions available
              </p>
            <% else %>
              <p>
                <i class="fas fa-exclamation-circle"></i> Consider adding a capsule image to improve your store page's visual appeal.
              </p>
            <% end %>
          </div>

          <%# Image validation report %>
          <% if @analysis.image_validation.present? %>
            <div class="content-block">
              <h6>Image Validation Report</h6>
              <div class="content-with-copy">
                <div class="content">
                  <ul>
                    <% @analysis.image_validation.split(/\n| - /).each do |validation| %>
                      <% if validation.strip.present? %>
                        <li><%= validation.strip.sub(/^*[^*]+?*:? ?/, '') %></li>
                      <% end %>
                    <% end %>
                  </ul>
                </div>
              </div>
              <small class="d-block mt-2">
                <i class="fas fa-robot"></i> This validation is based on <%= link_to "Steam's requirements for capsule images", "https://partner.steamgames.com/doc/store/assets/standard", target: "_blank" %>
              </small>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div> 